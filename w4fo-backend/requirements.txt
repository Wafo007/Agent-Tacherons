fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
# psycopg2-binary : driver SYNCHRONE utilisé UNIQUEMENT par Alembic (voir
# alembic/env.py). Wheel précompilé pour Windows/macOS/Linux : n'a pas besoin
# de compilateur C ni du Redistributable Visual C++ (contrairement à `psycopg2`
# non-binary). L'application elle-même continue d'utiliser asyncpg (async).
psycopg2-binary==2.9.9
pydantic==2.9.2
pydantic-settings==2.6.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==43.0.3
python-multipart==0.0.12
pgvector==0.3.6
langgraph==0.2.53
langchain-core==0.3.21
mistralai==1.2.5
typing-extensions==4.12.2
httpx==0.27.2
websockets==13.1
python-dotenv==1.0.1
apscheduler==3.10.4

# --- Voix (STT/TTS) ---
# SpeechRecognition : 100% Python pur, aucune extension C / DLL native.
# Utilisé avec la méthode recognize_google() (endpoint gratuit non officiel,
# voir src/infrastructure/voice/stt_provider_impl.py pour le compromis assumé).
# Remplace faster-whisper (retiré : dépendait de ctranslate2, qui nécessite le
# Visual C++ Redistributable sous Windows — cause de l'erreur ctranslate2.dll).
SpeechRecognition==3.10.4
edge-tts==6.1.19

# Dev / tests
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==6.0.0
ruff==0.7.4
